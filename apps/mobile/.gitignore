
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
# Native projects are generated by `expo prebuild` (Continuous Native
# Generation), not authored here — ios/ alone is ~290MB once Pods are
# installed. Everything that shapes them is tracked: app.json (bundle id,
# iPad orientations), react-native.config.js (the simdjson podspec
# WatermelonDB needs), and the dependency set. Regenerate with:
#   pnpm --filter mobile exec expo prebuild --platform ios
/ios/
/android/
